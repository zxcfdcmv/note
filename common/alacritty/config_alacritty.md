# 安装alacritty
```shell
paru -S alacritty
```
# 配置sway默认使用alacritty终端
```shell
# helix ~/.config/sway/config
set $term alacritty
```

# 配置alacritty配置文件
```shell
helix ~/.config/alacritty/alacritty.toml
```
